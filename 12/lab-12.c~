#include <stdio.h>
#include <stdlib.h>
#include "htable.h"

int main(void) {
   int item;
   htable my_htable = htable_new();
   
   /* while (1 == scanf("%d", &item)) { */
   /*    htable_append(my_flexarray, item); */
   /* } */
   
   htable_print(my_htable);
   htable_free(my_htable);
   
   return EXIT_SUCCESS;
}
